<%= form_for(@api_team) do |f| %>
  <% if @api_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_team.errors.count, "error") %> prohibited this api_team from being saved:</h2>

      <ul>
      <% @api_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
